/**
 * A model for the modal shift UI
 *
 * It's not quite flux because it's pretty simple, but could be replaced
 * with a full flux model fairly easily.
 */

import JourneyModel from './model/JourneyModel'
import JourneyModelSegment from './model/JourneyModelSegment'

export {JourneyModel, JourneyModelSegment}
